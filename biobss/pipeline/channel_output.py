from .bio_channel import Channel
import numpy as np
import pandas as pd
from .bio_data import Bio_Data
import inspect
"""
Write a method which takes
 * Another method,args,kwargs as input
 * Selects Channel and Event_Channel Classes from args and kwargs
 * Uses inspect to get the signature of the method
 * Uses the signature to determine the required arguments and their types
 * Converts Channel and Event_Channel classes to the required types
 * return's args and kwargs

"""





